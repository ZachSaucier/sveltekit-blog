# Copy to `.env` in the project root (never commit `.env`).
# Create an app password at https://bsky.app/settings/app-passwords

ATPROTO_HANDLE=zachsaucier.com
ATPROTO_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx

# Optional — set after first run if you prefer env over the manifest file
# ATPROTO_PUBLICATION_URI=at://did:plc:xxx/site.standard.publication/xxx
